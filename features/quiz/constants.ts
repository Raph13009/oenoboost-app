import type { QuizType } from "@/types/database";

export const QUIZ_TYPES: QuizType[] = ["beginner", "intermediate", "expert"];

export const QUIZ_SHELL_CLASS =
  "fixed inset-x-0 top-14 bottom-0 z-10 flex min-h-0 w-full flex-col overflow-hidden bg-background";
